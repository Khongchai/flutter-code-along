class Routes {
  final String route;

  /*
     Also consider simply adding routeName to each widget instead.
   */
  const Routes.categoryMeals() : route = "/category-meals";
  const Routes.mealDetail() : route = "/meal-detail";
  const Routes.filters() : route = "/filters";
}
