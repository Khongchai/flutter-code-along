import "package:flutter/material.dart";

class Colors {
  Color primary;
  Color accent;
  Color background;

  Colors(this.primary, this.accent, this.background);
}

final appColors = Colors(
    const Color(0xff084fff), const Color(0xfffecd00), const Color(0xff291467));
